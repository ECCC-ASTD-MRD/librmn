#include <WhiteBoard_constants.h>

use iso_fortran_env, only: int64

type :: whiteboard
    sequence
    integer(kind = int64) :: wb
end type whiteboard

#ifdef _OPENMP
#include <whiteboard_interface_omp.cdk>
#else
#include <whiteboard_interface_st.cdk>
#endif
